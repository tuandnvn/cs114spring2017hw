The simplest kind of separate system uses a single , self-contained unit .
The simplest kind separate . uses self-contained a of system , unit single
system self-contained kind a , separate uses of simplest unit single The .
simplest system kind unit separate single , uses of a The self-contained .
, system a unit simplest . The single of uses self-contained kind separate
self-contained separate of single . system uses unit The simplest , a kind
self-contained single . of simplest , uses The a kind system separate unit
system , single kind separate a of uses unit The simplest self-contained .
single of . self-contained unit uses , system a kind separate simplest The
unit of single system separate kind simplest The self-contained . a , uses
