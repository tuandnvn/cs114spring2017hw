hence Af and the minimal polynomial is simply x , which says the operator is 0 .
the 0 minimal , polynomial which x says is Af . is the hence simply operator and
. x is minimal Af and hence polynomial is says simply , operator 0 the which the
simply , is the says is which 0 operator polynomial Af x and minimal hence the .
operator hence and says polynomial 0 the which x is is minimal the , simply . Af
and x is hence is simply minimal says which , the Af polynomial 0 operator the .
and is operator , which is the says simply . Af polynomial hence x minimal the 0
is the . says Af is operator hence x minimal 0 which polynomial and the simply ,
operator 0 is which is the minimal . says and Af x polynomial , the simply hence
simply the says minimal is x 0 which , is Af polynomial hence and the . operator
