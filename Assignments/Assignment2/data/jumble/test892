What exactly are we trying to prove ? ?
? What are exactly trying we to ? prove
to trying we What ? prove ? exactly are
are prove ? we exactly trying What to ?
to exactly ? we prove ? trying are What
are prove ? exactly ? to trying we What
to trying prove ? ? exactly we are What
to prove exactly ? are What we ? trying
are to exactly prove ? we What ? trying
trying ? to ? exactly prove are What we
