What are the relevant data ? ?
What data ? are ? the relevant
? ? What relevant data are the
? the data ? relevant What are
relevant What ? ? data are the
relevant the are ? What data ?
data ? What ? relevant are the
? relevant ? data the are What
relevant What ? data are ? the
are What relevant data ? ? the
